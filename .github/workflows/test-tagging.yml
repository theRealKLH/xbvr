steps:
  - uses: oprypin/find-latest-tag@v1
    with:
      repository: xbapps/xbvr  # The repository to scan.
      releases-only: true  # We know that all relevant tags have a GitHub release for them.
    id: xbvr  # The step ID to refer to later.

  - run: echo "XBVR is at version ${{ steps.xbvr.outputs.tag }}"

 # - uses: actions/checkout@v2
 #   with:
 #     repository: octokit/rest.js  # Download that repository.
 #     ref: ${{ steps.octokit.outputs.tag }}  # At the latest released version, found earlier.
